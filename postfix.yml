---
# Playbook for postfix server


- name: Install postfix
  hosts: dorky
  roles:
    - postfix
  vars:
    postfix_raw_options:
      - |
        append_dot_mydomain = yes
        myorigin = /etc/mailname
    postfix_hostname: "dorylus.cc"
    postfix_mailname: "dorylus.cc"
